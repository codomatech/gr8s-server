/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  images: {
    loader: 'akamai',
    path: '',
//     loader: "default",
//     domains: ["localhost"],
  },
}

module.exports = nextConfig
